import React from "react";
import ReactDOM from "react-dom/client";
import ProductionDashboard from "./App.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProductionDashboard />
  </React.StrictMode>
);
